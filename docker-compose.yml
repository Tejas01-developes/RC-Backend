services:
  backend:
   build: .
   ports: 
    - "3000:3000"
   environment:
   - DB_USER=Run-club
   - DB_NAME=Runclub
   - DB_PASS=runfor
   - DB_HOST=db
   - DB_PORT=3306
   - ACCESS_KEY=hbrtngurhshdid
   - REFRESH_KEY=oitujnbrtvrtvhbyh
   - EMAIL=xyz@gmail.com
   - EMAIL_PASS=edrsersertfsrfsertb
   - REDIS_HOST=redis
   - REDIS_PORT=6379
   - RAZOR_API=erfgserfsefrbserer
   - RAZOR_SECRET=sergsegsertgaserg

   depends_on: 
    - db
    - redis


  db:

    image: mysql:8.0
    ports: 
    - "3306:3306"
    environments:
     - DB_USER=Run-club
     - DB_NAME=Runclub
     - DB_PASS=runfor
     - DB_HOST=db
     - DB_PORT=3306
    volumes:
    - mysql:/var/lib/mysql


  redis: 
    image: redis:alpine
    ports:
    - "6379:6379"

volumes:
    mysql:  
